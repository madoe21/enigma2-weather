#!/bin/sh
set -e
# Only remove data on real uninstall, not on upgrade/reinstall
if [ "$1" = "remove" ]; then
    rm -f /etc/enigma2/weather.json || true
    rm -f /etc/enigma2/openmeteo_weather.json || true
fi
exit 0
